{
  "mcpServers": {
    "mem0": {
      "command": "uv",
      "args": ["run", "${PLUGIN_ROOT}/scripts/mcp_proxy.py"],
      "env": {
        "MEM0_API_KEY": "${MEM0_API_KEY}",
        "MEM0_MCP_URL": "${MEM0_MCP_URL}"
      }
    }
  }
}
