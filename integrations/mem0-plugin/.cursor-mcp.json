{
  "mcpServers": {
    "mem0": {
      "command": "uv",
      "args": ["run", "${CURSOR_PLUGIN_ROOT}/scripts/mcp_proxy.py"],
      "env": {
        "MEM0_API_KEY": "${env:MEM0_API_KEY}",
        "MEM0_MCP_URL": "${env:MEM0_MCP_URL}"
      }
    }
  }
}
