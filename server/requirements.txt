fastapi>=0.139.0,<1.0.0
uvicorn>=0.50.0,<1.0.0
pydantic[email]>=2.12.0,<3.0.0
# mem0 — pip install -e ".[nlp]" from repo root
python-dotenv>=1.2.0,<2.0.0
mcp[cli]>=1.28.0,<2.0.0
psycopg>=3.3.0,<4.0.0
psycopg-pool>=3.3.0,<4.0.0

# Bundled LLM/embedder providers. Extend by adding the provider's package
# here, rebuilding the image, and updating BUNDLED_*_PROVIDERS in main.py.
anthropic>=0.39,<1.0
google-genai>=1.0.0,<2.0.0
ollama>=0.3.0

# Auth & database (Phase 1)
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
bcrypt>=4.0.1,<5.0
python-jose[cryptography]>=3.3,<4.0
cryptography>46.0.4
httpx>=0.27,<1.0

# Anonymous telemetry — on by default, opt out with MEM0_TELEMETRY=false
posthog>=3.0,<8.0

# Per-IP rate limiting on auth endpoints
slowapi>=0.1.9,<1.0

# Thread-safe TTL cache for synthetic event tracking
cachetools>=5.0,<8.0
